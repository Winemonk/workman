<UserControl
    x:Class="Workman.Apps.Views.StatisticsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Workman.Apps.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UniformGrid>
        <!--<lvc:CartesianChart
            Grid.Column="1"
            Series="{Binding SizeSeries}"
            TooltipTextPaint="{Binding TextPaint}"
            TooltipTextSize="12"
            XAxes="{Binding SizeXAxes}"
            YAxes="{Binding SizeYAxes}">
            <lvc:CartesianChart.Title>
                <ves:LabelVisual Text="迭代" TextSize="15" />
            </lvc:CartesianChart.Title>
        </lvc:CartesianChart>-->
    </UniformGrid>
</UserControl>
